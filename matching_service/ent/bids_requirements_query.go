// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"matching_service/ent/bids"
	"matching_service/ent/bids_requirements"
	"matching_service/ent/predicate"
	"matching_service/ent/requirements"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BidsRequirementsQuery is the builder for querying Bids_Requirements entities.
type BidsRequirementsQuery struct {
	config
	ctx              *QueryContext
	order            []bids_requirements.OrderOption
	inters           []Interceptor
	predicates       []predicate.Bids_Requirements
	withBids         *BidsQuery
	withRequirements *RequirementsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BidsRequirementsQuery builder.
func (_q *BidsRequirementsQuery) Where(ps ...predicate.Bids_Requirements) *BidsRequirementsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BidsRequirementsQuery) Limit(limit int) *BidsRequirementsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BidsRequirementsQuery) Offset(offset int) *BidsRequirementsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BidsRequirementsQuery) Unique(unique bool) *BidsRequirementsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BidsRequirementsQuery) Order(o ...bids_requirements.OrderOption) *BidsRequirementsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBids chains the current query on the "bids" edge.
func (_q *BidsRequirementsQuery) QueryBids() *BidsQuery {
	query := (&BidsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bids_requirements.Table, bids_requirements.FieldID, selector),
			sqlgraph.To(bids.Table, bids.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bids_requirements.BidsTable, bids_requirements.BidsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRequirements chains the current query on the "requirements" edge.
func (_q *BidsRequirementsQuery) QueryRequirements() *RequirementsQuery {
	query := (&RequirementsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bids_requirements.Table, bids_requirements.FieldID, selector),
			sqlgraph.To(requirements.Table, requirements.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bids_requirements.RequirementsTable, bids_requirements.RequirementsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Bids_Requirements entity from the query.
// Returns a *NotFoundError when no Bids_Requirements was found.
func (_q *BidsRequirementsQuery) First(ctx context.Context) (*Bids_Requirements, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bids_requirements.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BidsRequirementsQuery) FirstX(ctx context.Context) *Bids_Requirements {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Bids_Requirements ID from the query.
// Returns a *NotFoundError when no Bids_Requirements ID was found.
func (_q *BidsRequirementsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bids_requirements.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BidsRequirementsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Bids_Requirements entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Bids_Requirements entity is found.
// Returns a *NotFoundError when no Bids_Requirements entities are found.
func (_q *BidsRequirementsQuery) Only(ctx context.Context) (*Bids_Requirements, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bids_requirements.Label}
	default:
		return nil, &NotSingularError{bids_requirements.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BidsRequirementsQuery) OnlyX(ctx context.Context) *Bids_Requirements {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Bids_Requirements ID in the query.
// Returns a *NotSingularError when more than one Bids_Requirements ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BidsRequirementsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bids_requirements.Label}
	default:
		err = &NotSingularError{bids_requirements.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BidsRequirementsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Bids_RequirementsSlice.
func (_q *BidsRequirementsQuery) All(ctx context.Context) ([]*Bids_Requirements, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Bids_Requirements, *BidsRequirementsQuery]()
	return withInterceptors[[]*Bids_Requirements](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BidsRequirementsQuery) AllX(ctx context.Context) []*Bids_Requirements {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Bids_Requirements IDs.
func (_q *BidsRequirementsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(bids_requirements.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BidsRequirementsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BidsRequirementsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BidsRequirementsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BidsRequirementsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BidsRequirementsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BidsRequirementsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BidsRequirementsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BidsRequirementsQuery) Clone() *BidsRequirementsQuery {
	if _q == nil {
		return nil
	}
	return &BidsRequirementsQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]bids_requirements.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.Bids_Requirements{}, _q.predicates...),
		withBids:         _q.withBids.Clone(),
		withRequirements: _q.withRequirements.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithBids tells the query-builder to eager-load the nodes that are connected to
// the "bids" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BidsRequirementsQuery) WithBids(opts ...func(*BidsQuery)) *BidsRequirementsQuery {
	query := (&BidsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBids = query
	return _q
}

// WithRequirements tells the query-builder to eager-load the nodes that are connected to
// the "requirements" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BidsRequirementsQuery) WithRequirements(opts ...func(*RequirementsQuery)) *BidsRequirementsQuery {
	query := (&RequirementsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRequirements = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BidsRequirements.Query().
//		GroupBy(bids_requirements.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BidsRequirementsQuery) GroupBy(field string, fields ...string) *BidsRequirementsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BidsRequirementsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = bids_requirements.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.BidsRequirements.Query().
//		Select(bids_requirements.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *BidsRequirementsQuery) Select(fields ...string) *BidsRequirementsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BidsRequirementsSelect{BidsRequirementsQuery: _q}
	sbuild.label = bids_requirements.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BidsRequirementsSelect configured with the given aggregations.
func (_q *BidsRequirementsQuery) Aggregate(fns ...AggregateFunc) *BidsRequirementsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BidsRequirementsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !bids_requirements.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BidsRequirementsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Bids_Requirements, error) {
	var (
		nodes       = []*Bids_Requirements{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withBids != nil,
			_q.withRequirements != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Bids_Requirements).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Bids_Requirements{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBids; query != nil {
		if err := _q.loadBids(ctx, query, nodes, nil,
			func(n *Bids_Requirements, e *Bids) { n.Edges.Bids = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRequirements; query != nil {
		if err := _q.loadRequirements(ctx, query, nodes, nil,
			func(n *Bids_Requirements, e *Requirements) { n.Edges.Requirements = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BidsRequirementsQuery) loadBids(ctx context.Context, query *BidsQuery, nodes []*Bids_Requirements, init func(*Bids_Requirements), assign func(*Bids_Requirements, *Bids)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Bids_Requirements)
	for i := range nodes {
		fk := nodes[i].BidsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bids.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bids_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BidsRequirementsQuery) loadRequirements(ctx context.Context, query *RequirementsQuery, nodes []*Bids_Requirements, init func(*Bids_Requirements), assign func(*Bids_Requirements, *Requirements)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Bids_Requirements)
	for i := range nodes {
		fk := nodes[i].RequirementsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(requirements.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "requirements_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *BidsRequirementsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BidsRequirementsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bids_requirements.Table, bids_requirements.Columns, sqlgraph.NewFieldSpec(bids_requirements.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bids_requirements.FieldID)
		for i := range fields {
			if fields[i] != bids_requirements.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withBids != nil {
			_spec.Node.AddColumnOnce(bids_requirements.FieldBidsID)
		}
		if _q.withRequirements != nil {
			_spec.Node.AddColumnOnce(bids_requirements.FieldRequirementsID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BidsRequirementsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(bids_requirements.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = bids_requirements.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BidsRequirementsGroupBy is the group-by builder for Bids_Requirements entities.
type BidsRequirementsGroupBy struct {
	selector
	build *BidsRequirementsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BidsRequirementsGroupBy) Aggregate(fns ...AggregateFunc) *BidsRequirementsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BidsRequirementsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BidsRequirementsQuery, *BidsRequirementsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BidsRequirementsGroupBy) sqlScan(ctx context.Context, root *BidsRequirementsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BidsRequirementsSelect is the builder for selecting fields of BidsRequirements entities.
type BidsRequirementsSelect struct {
	*BidsRequirementsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BidsRequirementsSelect) Aggregate(fns ...AggregateFunc) *BidsRequirementsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BidsRequirementsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BidsRequirementsQuery, *BidsRequirementsSelect](ctx, _s.BidsRequirementsQuery, _s, _s.inters, v)
}

func (_s *BidsRequirementsSelect) sqlScan(ctx context.Context, root *BidsRequirementsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
